import { BasePage } from '../base.page';
/**
 * The Internet - Large & Deep DOM page.
 *
 * For now we only keep the URL (no dedicated selectors yet).
 */
export class LargePage extends BasePage {
  protected pageUrl = '/large';
}
