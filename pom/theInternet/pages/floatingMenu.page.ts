import { BasePage } from '../base.page';
/**
 * The Internet - Floating Menu page.
 *
 * For now we only keep the URL (no dedicated selectors yet).
 */
export class FloatingMenuPage extends BasePage {
  protected pageUrl = '/floating_menu';
}
